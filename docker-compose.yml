version: '3.8'

services:
  glossary-grpc:
    build: .
    ports:
      - "50051:50051"
    volumes:
      - glossary_data:/app/data
    environment:
      - DATABASE_URL=sqlite:///./data/glossary.db
    restart: unless-stopped

volumes:
  glossary_data: